spring:
  cloud:
    config:
      server:
        git:
          uri: ${CONFIG_SERVER_GIT_URI}
          password: ${CONFIG_SERVER_GIT_PASSWORD}
          username: ${CONFIG_SERVER_GIT_USERNAME}
          force-pull: true
server:
  port: 8888